import { DocumentList } from '../components/DocumentList';

export default function InsurancePage() {
  return (
    <DocumentList
      kind="rca"
      title="Insurance (RCA)"
      description="Active, expiring, and expired RCA policies — with photos of every document."
    />
  );
}
